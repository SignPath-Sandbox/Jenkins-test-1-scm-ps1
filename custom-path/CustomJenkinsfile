pipeline {
    agent any

    stages {
        stage('Create PowerShell file') {
            steps {
                sh '''
                    mkdir -p build-output
                    echo 'Write-Host "Hello World 2 - revision 3 - main branch"' > build-output/script.ps1
                    ls -lh build-output
                    cat build-output/script.ps1
                '''
            }
        }
    }
}
